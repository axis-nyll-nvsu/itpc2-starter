# Starter Project - Back-End

To install:  
- `pip install flask mysql-connector-python python-dotenv`

To configure:  
- Create the database found in `schema.sql`
- Copy `.env.example` into a new file `.env`

To run the application:  
- `flask run`